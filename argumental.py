import numpy as np

class Solver:
	def __init__(self):
		self.args = dict()

	def prepare_attack_matrix(self):
		n=len(self.args)
		self.attack_matrix=np.zeros(n,n)
	def extensions(self):
		# n le nombre de arguments
		result=[]
		for i in range(len(self.args)):
			pass
	def function_gamma(self):
		# 
		pass
